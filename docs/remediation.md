# 技术复核：计算器与完整生命周期

状态：进行中。2026-09-21 收到新的技术复核反馈，本轮结果独立记录，不沿用历史用例的 PASS 代替本轮实际操作。

## 修复目标

已在定向读取的存量计算器 v1/v2/v3 源码与真实 Chromium 回放中确认：左操作数经 formatNumber 变为字符串，右操作数为 number，导致加法拼接为 123。修复生成协议，要求运算状态保持数值、输入显式转换、显示格式化不写回运算状态，并自检四则运算与连续输入；重新调用真实模型进行业务验证。`eval`/`Function` 与 CSP 不兼容是独立防错项，不是该样本根因；保持 CSP 不变，不开放 unsafe-eval。原评审项目链接尚未提供，不将数据库名称匹配自动等同于评审身份，也不直接修改该项目。

## 本轮验收

全部业务步骤使用真实 DeepSeek、实际工作台、真实数据库和浏览器。同一个计算器项目完成连续版本链；每阶段保存输入、实际输出、截图、源码快照、项目/Run/Version ID、调用用量及数据库数据 revision。

| ID | 操作与通过条件 | 当前状态 |
|---|---|---|
| R-01 | 新建中文计算器。实际点击计算 `12+3=15`、`12-3=9`、`12×3=36`、`12÷3=4`，验证清空表达式、历史记录内容和刷新保持。补查优先级、括号、负数、除零提示（按生成需求明确行为）。不能只看页面出现或版本发布。 | NOT_RUN |
| R-02 | 同一项目依次修改两轮得到 v2/v3；分别验证新增功能，并完整重跑四则运算、清空和已有历史，不接受只验证新功能。 | NOT_RUN |
| R-03 | 保存 v3 的项目、对话、三份源码、业务数据、版本与当前 Preview；关闭整个专用浏览器进程，再用同一持久化 profile 启动，不手工回填 token/storageState。核对原身份、原项目及上述六类内容恢复，实际继续计算并保存。 | NOT_RUN |
| R-04 | 记录 v1 源码及 Preview；从 v3 恢复 v1，产生 v4。v4 三份源码/哈希必须等于 v1，正式 Preview 的可见行为对应 v1，业务数据保留。随后修改得到 v5；核对 Run.baseVersionId=v4、生成上下文读取恢复源码，功能符合恢复分支且旧增量功能不会无故出现。 | NOT_RUN |
| R-05 | 导出 v5 的 HTML，以 file: 独立打开并实际计算；通过本地 HTTP 打开并验证历史保存与刷新。导出初始不带平台数据，导出端新增历史不会修改云端；另一项目/匿名访客及另一项目导出数据均隔离。 | NOT_RUN |

用户已明确 R-03 的含义为“同一浏览器关闭重开，保留身份即可”；不增加跨设备找回或清除身份后的恢复能力。

## 结果规则

- 模型真实调用和功能实际正确同时满足，才记录业务 PASS；fixture 仅用于确定性故障与工程回归。
- 如普通需求未复现评审故障，保留该事实，并定向复现失效代码路径；不编造与评审使用同一源码的证据。
- 每次失败保留原始结果。定位器失败与应用错误分别记录，不能通过修改测试期待把业务错误改成 PASS。
- 当前轮全部通过后，再发布修复并在生产入口验证受影响流程；不代用户发送新的补救材料。
