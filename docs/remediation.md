# 技术复核：计算器与完整生命周期

状态：本地 R-01～05 已通过，生产复验待执行。2026-09-21 收到新的技术复核反馈，本轮结果独立记录，不沿用历史用例的 PASS 代替本轮实际操作。

评审随后补充的精确复现：进入 Preview，依次点击 `1 → 2 → + → 3 → =`，显示 `123`，历史为 `12 + 3 = 123`。本轮每个接受验收的版本都要同时检查显示值和**本次新增**历史为 `15`，不能借用之前的正确历史记录通过检查。

## 修复目标

已在定向读取的存量计算器 v1/v2/v3 源码与真实 Chromium 回放中确认：左操作数经 formatNumber 变为字符串，右操作数为 number，导致加法拼接为 123。修复生成协议，要求运算状态保持数值、输入显式转换、显示格式化不写回运算状态，并自检四则运算与连续输入；重新调用真实模型进行业务验证。`eval`/`Function` 与 CSP 不兼容是独立防错项，不是该样本根因；保持 CSP 不变，不开放 unsafe-eval。原评审项目链接尚未提供，不将数据库名称匹配自动等同于评审身份，也不直接修改该项目。

## 本轮验收

全部业务步骤使用真实 DeepSeek、实际工作台、真实数据库和浏览器。同一个计算器项目完成连续版本链；每阶段保存输入、实际输出、截图、源码快照、项目/Run/Version ID、调用用量及数据库数据 revision。

| ID | 操作与通过条件 | 当前状态 |
|---|---|---|
| R-01 | 新建中文计算器。实际点击计算 `12+3=15`、`12-3=9`、`12×3=36`、`12÷3=4`，验证清空表达式、历史记录内容和刷新保持。补查优先级、括号、负数、除零提示（按生成需求明确行为）。不能只看页面出现或版本发布。 | 本地 PASS；生产 NOT_RUN |
| R-02 | 同一项目依次修改两轮得到 v2/v3；分别验证新增功能，并完整重跑四则运算、清空和已有历史，不接受只验证新功能。 | 本地 PASS；生产 NOT_RUN |
| R-03 | 保存 v3 的项目、对话、三份源码、业务数据、版本与当前 Preview；关闭整个专用浏览器进程，再用同一持久化 profile 启动，不手工回填 token/storageState。核对原身份、原项目及上述六类内容恢复，实际继续计算并保存。 | 本地 PASS；生产 NOT_RUN |
| R-04 | 记录 v1 源码及 Preview；从 v3 恢复 v1，产生 v4。v4 三份源码/哈希必须等于 v1，正式 Preview 的可见行为对应 v1，业务数据保留。随后修改得到 v5；核对 Run.baseVersionId=v4、生成上下文读取恢复源码，功能符合恢复分支且旧增量功能不会无故出现。 | 本地 PASS；生产 NOT_RUN |
| R-05 | 导出 v5 的 HTML，以 file: 独立打开并实际计算；通过本地 HTTP 打开并验证历史保存与刷新。导出初始不带平台数据，导出端新增历史不会修改云端；另一项目/匿名访客及另一项目导出数据均隔离。 | 本地 PASS；生产 NOT_RUN |

用户已明确 R-03 的含义为“同一浏览器关闭重开，保留身份即可”；不增加跨设备找回或清除身份后的恢复能力。

## 本地真实结果（2026-09-21）

产品代码 `5a8fdef04b3d681c2eed17d3716678914af69b92`，本地工作台连接专用远程 Supabase 与真实 `deepseek-flash`，Chromium 153。项目 `3dae236e-be58-4a6d-bae7-0434db3cfadd`，四次生成请求、8 次模型调用、115489 tokens；恢复不调用模型。[脱敏证据及阶段截图](../artifacts/verification/remediation-local-5a8fdef/summary.json)。

| 项目 | 实际结果 |
|---|---|
| R-01 | PASS。实际按钮操作得到 15、9、36、4，清空后为 0，四条历史刷新保留。 |
| R-02 | PASS。v2 加贪吃蛇、v3 加主题；每轮重验四则和本次新增历史，原历史保留。验证蛇头方向、暂停后棋盘稳定、重开，以及主题背景/文字颜色变化和保存。 |
| R-03 | PASS。关闭专用浏览器进程，再以原 profile 启动；原身份 GET 项目 200，项目/对话/源码/版本/业务数据一致，Preview 恢复；重新计算后数据 revision 增长。 |
| R-04 | PASS。v4 恢复自 v1，三份源码和哈希一致，历史 Preview 与正式 Preview 一致，业务数据保留。随后仅修改标题得到 v5，parent/base 均为 v4，未带回 v2/v3 的游戏和主题控件。 |
| R-05 | PASS（正常按键节奏）。独立 file HTML 可计算；HTTP 独立页刷新保留四条历史；导出与云端互不影响，同 origin 的另一项目导出清空后不影响本项目，全新匿名身份读原项目返回 404。 |

这是同项目、同身份的断点续验，不是单次不中断的全量运行。页面定位与像素测量适配失败均保留，没有为获得通过而重新生成项目或替换源码。v1 与 v4 的源码哈希均为 `2204af316ca6305efce1ce1714b36f42b0e416c4c67c7315963d7a3e2722b31e`。

导出快速点击实验曾触发原 Spec 的每秒 10 条存储消息限制：第四笔保存收到 `BRIDGE_RATE_LIMIT`，页面明确提示保存失败，实际保存三条。正常功能复验采用每键 100ms 的点击节奏；没有取消限流、隐藏错误或把失败保存标记为成功。该限制仍存在。括号等额外运算边界不包含在本轮四则验收结论中。

工程检查：127 单元测试、ESLint、TypeScript 通过。新验收入口为 `playwright.remediation.config.ts`；必须显式指定目标 URL、commit、独立 checkpoint、专用数据库身份和已拥有的第二项目导出 profile，使用真实模型，默认不执行、不重试生成。原评审项目身份尚未确认，存量项目不会被本次部署自动改写。

## 结果规则

- 模型真实调用和功能实际正确同时满足，才记录业务 PASS；fixture 仅用于确定性故障与工程回归。
- 如普通需求未复现评审故障，保留该事实，并定向复现失效代码路径；不编造与评审使用同一源码的证据。
- 每次失败保留原始结果。定位器失败与应用错误分别记录，不能通过修改测试期待把业务错误改成 PASS。
- 当前轮全部通过后，再发布修复并在生产入口验证受影响流程；不代用户发送新的补救材料。
