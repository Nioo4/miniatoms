# 2026-09-22 风险优化与再次复核

状态：候选 `3a22dc789425bd0b158bb066d31d3cd02456b067` 的生产真实模型 N-01～05 已通过；完整 CI 的一条旧错误文案断言已更新，等待重跑。本轮开发子 agent 为 GPT-5.6 Luna / max；主 agent 负责方案、代码审查和最终验收。上一轮结果保持原样，不作为本次代码的自动通过凭据。

## 变更目标

- 匿名身份：检查同一浏览器持久化身份的恢复；可选邮箱绑定/登录已实现，但生产入口默认关闭，真实邮箱及跨浏览器登录尚未验收。启用前置条件见 [邮箱说明](email-auth.md)。不能通过复制 token 或 service-role 越权读取冒充身份恢复。
- 保存请求：在共享 SDK 内对短时连续请求做有界调度，保留宿主安全限流、错误和超时；不静默合并或丢弃写入，不重试结果不确定的写入。
- 精确操作：通过可见按钮点击，不通过调用生成函数或直接写数据库制造运算结果。

## 本轮必须执行

| 编号 | 通过条件 | 结果 |
|---|---|---|
| N-01 | 同项目逐版本实际点击1→2→+→3→=，显示和本次新增历史均为15；同时通过减法、乘法、除法、清空和历史持久化。 | PASS：v1～v5均验证15、9、36、4；清空显示0，历史保留。 |
| N-02 | 同一项目连续两轮增量后重验旧功能及旧数据，按重复次数核对历史保留。 | PASS：v2增加游戏，v3增加主题；旧历史分别5→5、13→13，缺失0。含一次规划失败后的明确重试，见下文。 |
| N-03 | 关闭整个专用浏览器进程，再以原profile重开；项目、对话、三份源码、业务数据、版本和Preview全部恢复，仍能保存。 | PASS：原身份读取项目200，六类内容一致，重开后四则与保存成功。 |
| N-04 | v3恢复v1为v4，源码和哈希与v1一致，Preview同步且业务数据保留；v5后续修改的parent/base必须为v4。 | PASS：v1/v4哈希一致、逐文件源码与Preview一致；v5只改标题，parent/base指向v4，29条历史保留。 |
| N-05 | 实际UI导出HTML；file:和HTTP分别计算、刷新并检查数据保留；导出/云端、不同项目导出与匿名访客之间隔离。 | PASS：两种打开方式均四则正确、刷新保留4条；同HTTP origin另项目清空不影响本项目；云端不变，新访客404。另补file跨项目隔离。 |
| N-06 | 共享SDK短时存储burst无需人为按键延迟即可保存；过量并发明确拒绝、超时未发送项不晚发、旧frame不写入新版本；恶意直接消息仍限流。 | PASS（确定性浏览器 fixture，业务复验另列） |

业务验收使用真实模型、真实浏览器与数据库。确定性故障/协议测试单独标明fixture。所有失败保留，测试定位适配可以同项目断点续验，不重新生成来替换已产出的失败应用。SDK排队不等于为任意生成程序提供读改写事务；若生成应用自身并发覆盖记录，须单独记录和修复。

## 存储通道工程检查

26 项预览浏览器测试通过；随后新增的超时用例单独通过。该用例令首笔宿主写入延迟 11 秒：10 秒时首笔和排队请求均明确超时，宿主仅收到一条写入；迟到回执到达后再次保存仍被拒绝。已发送的首笔可能成功落盘，因此不把超时描述为“未发生写入”。

SDK 的 get/set 共用单在途 FIFO，最多 10 个排队与在途请求，发送间隔至少 125ms；每项自入队起 10 秒到期。宿主安全限流保持不变。版本切换冻结后，尚未到达宿主的写入会被拒绝；现有 drain 只等待宿主已接收请求，不承诺离开前的全部点击均自动保存。

## 候选验证

本地 131 项单元测试、类型检查、ESLint（零警告）、生产构建通过。客户端原有 18 项浏览器 fixture 通过，新增同 UID 邮箱状态用例修正定位器后单独通过。这些是工程检查，不等同于真实 SMTP 或真实模型验收。

本轮生产真实模型链只使用一个新建项目，模型测试开关为 off，按键额外延迟为 0ms。每笔运算除可见显示与新增历史外，还检查数据库或导出 localStorage 内对应记录的新增次数；每次失败保留独立 attempt，并通过原 profile 与 checkpoint 续验。

## 本轮生产证据与边界

项目 `d542f8bc-65f7-41fa-98a0-ba23e2bfb910`，真实 `deepseek-flash`，Chromium 153。共9次模型调用、127239 tokens；最终5个源码版本、业务revision 37、33条计算历史。七次同项目断点续验汇总通过，不是单次不中断全量通过。[脱敏结果和阶段截图](../artifacts/verification/remediation-next-round-3a22dc78/summary.json)、[精确按键同屏截图](../artifacts/verification/remediation-next-round-3a22dc78/exact-path-12-plus-3-equals-15.png)。

v1与恢复出的v4重新计算的源码哈希同为 `26ecea97f89036fece5c76583d9e3c3b3e9b2437f1f4ef4f5d113af6d8020e64`。v4为 `9a1129a4-efb2-40c5-9e1e-a4016d0b43f0`；后续v5为 `9b2a2837-880d-47e9-aa63-27b8f16de0b6`，其parent及生成Run的base均为v4。各版本本次新增历史均独立核对，不借用既有正确记录。

保留的中断包括：历史结构从result适配为实际resultValue/resultText；两次画布颜色观测适配；重开后的重复需求按实际条数验证；一次模型规划功能条目128字超过120字限制。最后一项是实际模型失败，Run `b768b7e4-77a0-4021-810e-f11510454aac` 保留，未产出代码，v2未变。核实原因后仅明确重试一次原修改需求，随后产出v3；不是自动无限重试或替换不满意的生成应用。

邮箱恢复仍为 **NOT_RUN（生产真实邮箱）**：实现了原匿名UID绑定邮箱、验证码、密码与登录流程，工程fixture通过；默认开关为false。尚未验证SMTP、收信和新浏览器密码登录，因此清除未绑定的匿名身份后仍无法找回。启用前按邮箱说明完成真实验证。原评审旧项目源码不会被平台部署自动改写。
