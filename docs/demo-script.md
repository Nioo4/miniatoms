# 已录制的本地真实模型演示

约3分钟本地真实演示已完成录制，8个业务阶段PASS。浏览器解码时长179.92秒、1440×1000；阶段时间及取样核查见[脱敏录像元数据](../artifacts/verification/local-demo/summary.json)。交付包文件名为 `MiniAtoms-本地真实模型演示.webm`，另附 `MiniAtoms-本地真实模型演示.zh-CN.srt` 字幕；视频不进入公开Git，不公开私有绝对路径。

录像中的工作台运行于localhost，连接真实DeepSeek和远程Supabase。它是一条独立演示：v1生成→一次修改v2→恢复v3，共4次真实模型调用；不是完整验收主链v1～v5，不是生产LIVE-11。

## 实际录制内容

1. 从空数据真实生成中文求职投递看板，展示规划、候选检查及v1。
2. 新增两条虚构投递记录，展示保存结果。
3. 按面试阶段筛选，核对记录与统计。
4. 真实修改：增加公司搜索和深色风格，保留记录，生成v2。
5. 刷新工作台，核对云端保存的两条记录。
6. 恢复第一版，产生v3并保留当前业务数据；恢复本身不调用模型。
7. 导出独立HTML并实际添加虚构记录。
8. 切换390px手机视图，展示应用成果和记录。

结尾为明确的结果停留展示，不冒充额外业务操作或模型处理时间。等待和演示速度以实际录像为准。

## 与验收的关系

本地LIVE-01～10已分组真实通过：[2236e1a主流程](../artifacts/verification/live-remote-2236e1a/live-results.json)覆盖01～07，[ef591ce独立流程](../artifacts/verification/live-remote-ef591ce/live-results.json)覆盖08～10；产品源码和迁移树一致，非同次全量PASS。视频是独立交付证据，不替代上述完整操作记录。

独立[生产LIVE-11](../artifacts/verification/live-production-91ccda8/)已通过，但本视频仍为本地录屏。个人字段已在私有副本补齐，视频提交渠道和最终提交仍由本人处理；不得将本地录像称为公开生产运行，也不得用fixture画面替换真实生成片段。
